{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // matches the es2022 build target: every browser with custom elements has it
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src", "test"]
}
