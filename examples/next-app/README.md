# example-next-app

A Next.js (App Router, TypeScript) example that renders `@nurblings/react`
avatars entirely on the server: a grid of avatars, a size row, a background
shape row, and one hover-animated avatar.

## Run it

```sh
pnpm --filter example-next-app dev
pnpm --filter example-next-app build
```
