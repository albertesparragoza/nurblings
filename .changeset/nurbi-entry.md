---
"nurblings": minor
---

Nurbi's stored drawing moves to its own entry: `nurbi(options)` from `nurblings/nurbi`. The seed `nurbi` is now an ordinary Nurbling and `isFlagshipSeed` is removed, so every other import is about 1 KB smaller compressed.
