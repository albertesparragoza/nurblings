---
"nurblings": minor
---

Cleaner faces and accessories. Eyes no longer carry a highlight and the dot mouth is gone (its seeds draw no mouth), so nothing on a face reads as a mark on the skin. Scarves, hats and collars follow each side of the body exactly, on every silhouette. In `mode: 'auto'` the nearest `data-theme` or `.dark`/`.light` marker wins at any depth, through CSS `@scope`.
