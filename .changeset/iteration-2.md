---
"nurblings": minor
---

`nurblingSrc(seed, opts)` returns an avatar as a data URI for an `<img src>` in one call, and `createNurblings` instances get the same as `.src()`. The badge extra is gone: seeds that drew it now draw no extra, and every other seed is unchanged. Named themes never recolour Nurbi.
