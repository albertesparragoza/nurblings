---
"nurblings": patch
---

Custom body designs are checked when an instance is created: finite numbers, a known plate zone and at most 12 plate rows and columns. `renderTraits()` applies the same plate grid limit.
